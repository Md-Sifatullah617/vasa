import 'package:flutter/material.dart';

class JoinMettingScreen extends StatelessWidget {
  const JoinMettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Join Meeting Screen"),
      ),
    );
  }
}
